import React from "react";
import "./Loading.scss";

const Loading = () => {
    return (
        <div className="loading">
            <h4 className="loading_title">Loading...</h4>
        </div>
    );
};

export default Loading;
